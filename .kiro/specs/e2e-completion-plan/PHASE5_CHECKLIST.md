# Phase 5: 개별 테스트 스펙 수정 체크리스트

## 📋 개요
- **우선순위**: 중간
- **예상 소요 시간**: 4-6시간
- **상태**: 🟡 진행중
- **의존성**: Phase 1-4 완료 권장

---

## 5.1 인증 테스트 수정

### 파일: `frontend/tests/e2e/specs/auth/session.spec.ts`

#### 5.1.1 토큰 만료 테스트 수정
- [ ] 현재 코드 분석
- [ ] 리다이렉트 URL 패턴 수정 (`**/` → `**/login**`)
- [ ] 테스트 완료

---

## 5.2 테이블 테스트 수정

### 파일: `frontend/tests/e2e/specs/table/*.spec.ts`

#### 5.2.1 seating.spec.ts 수정
- [ ] 현재 코드 분석
- [ ] 바이인 모달 대기 로직 추가
- [ ] 좌석 상태 확인 로직 추가
- [ ] 테스트 완료

---

#### 5.2.2 multiplayer.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 멀티플레이어 fixture 사용 ✅ 이미 사용중
- [x] 게임 시작 대기 로직 추가 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.3 blinds-button.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `maxPlayers` → `maxSeats` 수정 ✅ 완료 (이전 대화에서)
- [x] 딜러 버튼 위치 확인 로직 ✅ 이미 구현됨
- [x] 블라인드 마커 확인 로직 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.4 street-transitions.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 치트 API로 페이즈 전환 테스트 ✅ 이미 구현됨
- [x] 커뮤니티 카드 표시 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.5 timer.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `turnTimeSeconds` 옵션 제거 ✅ 완료 (이전 대화에서)
- [x] 타이머 UI 요소 확인 ✅ 이미 구현됨
- [x] 자동 폴드 테스트 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.6 showdown.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `maxPlayers` → `maxSeats` 수정 ✅ 완료
- [x] 카드 문자열을 `parseCard`/`parseCards` 함수 사용으로 변경 ✅ 완료
- [x] 쇼다운 하이라이트 컴포넌트 확인 ✅ 이미 구현됨
- [x] 승자 표시 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.7 side-pots.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `maxPlayers` → `maxSeats` 수정 ✅ 완료
- [x] `userId` → `user.id` 수정 ✅ 완료
- [x] 카드 문자열을 `parseCards` 함수 사용으로 변경 ✅ 완료
- [x] 사이드 팟 UI 요소 확인 ✅ 이미 구현됨
- [x] 팟 분배 로직 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.2.8 player-departure.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `maxPlayers` → `maxSeats` 수정 ✅ 완료
- [x] 플레이어 퇴장 시나리오 ✅ 이미 구현됨
- [x] 자동 폴드 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

## 5.3 피망 스타일 테스트 수정

### 파일: `frontend/tests/e2e/specs/pmang/*.spec.ts`

#### 5.3.1 hand-ranking.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 족보 가이드 컴포넌트 선택자 수정 ✅ 이미 올바름
- [x] 테스트 완료

---

#### 5.3.2 betting-buttons.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 팟 비율 버튼 선택자 수정 ✅ 이미 올바름
- [x] 테스트 완료

---

#### 5.3.3 showdown-highlight.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `maxPlayers` → `maxSeats` 수정 ✅ 완료
- [x] 카드 문자열을 `parseCard`/`parseCards` 함수 사용으로 변경 ✅ 완료
- [x] 쇼다운 하이라이트 선택자 수정 ✅ 이미 올바름
- [x] 테스트 완료

---

#### 5.3.4 card-squeeze.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료
- [x] `squeezeEnabled` 옵션 제거 ✅ 완료
- [x] 카드 쪼기 선택자 수정 ✅ 이미 올바름
- [x] 테스트 완료

---

## 5.4 복구 테스트 수정

### 파일: `frontend/tests/e2e/specs/recovery/*.spec.ts`

#### 5.4.1 reconnect.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] WebSocket 재연결 테스트 ✅ 이미 구현됨
- [x] 상태 복구 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.4.2 server-recovery.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 서버 재시작 시뮬레이션 테스트 ✅ 이미 구현됨
- [x] 핸드 복구 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

## 5.5 보안 테스트 수정

### 파일: `frontend/tests/e2e/specs/security/*.spec.ts`

#### 5.5.1 card-exposure.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 카드 노출 방지 테스트 ✅ 이미 구현됨
- [x] WebSocket 메시지 검증 ✅ 이미 구현됨
- [x] 테스트 완료

---

#### 5.5.2 server-authority.spec.ts 수정
- [x] 현재 코드 분석 ✅ 완료 (수정 불필요)
- [x] 서버 권한 검증 테스트 ✅ 이미 구현됨
- [x] 부정 행위 방지 확인 ✅ 이미 구현됨
- [x] 테스트 완료

---

## ✅ Phase 5 완료 체크포인트

```bash
# 전체 테이블 테스트 실행
cd frontend
npm run test:e2e -- --grep "Table" --project=chromium

# 피망 스타일 테스트 실행
npm run test:e2e -- --grep "피망" --project=chromium

# 복구 테스트 실행
npm run test:e2e -- --grep "Recovery" --project=chromium

# 보안 테스트 실행
npm run test:e2e -- --grep "Security" --project=chromium
```

---

## 📝 작업 노트

### 2026-01-13 수정 완료 항목:
1. `showdown-highlight.spec.ts` - `maxPlayers: 4` → `maxSeats: 4` 수정
2. `card-squeeze.spec.ts` - `squeezeEnabled: true` 옵션 제거
3. `showdown.spec.ts` - `maxPlayers: 4` → `maxSeats: 4` 수정, 카드 문자열을 `parseCard`/`parseCards` 사용으로 변경
4. `side-pots.spec.ts` - `maxPlayers: 6` → `maxSeats: 6` 수정, `userId` → `user.id` 수정, 카드 문자열을 `parseCards` 사용으로 변경
5. `player-departure.spec.ts` - `maxPlayers: 4` → `maxSeats: 4` 수정

### 주요 변경 사항:
- cheat-api의 `createTestTable` 인터페이스에 맞게 `maxPlayers` → `maxSeats`로 통일
- cheat-api의 `createTestTable` 인터페이스에 없는 옵션들 제거 (`turnTimeSeconds`, `squeezeEnabled`)
- n-player fixture의 `PlayerSession` 인터페이스에 맞게 `userId` → `user.id`로 수정
- 카드 타입 오류 해결을 위해 `parseCard`/`parseCards` 함수 사용

### 분석 결과 수정 불필요한 파일들:
- `multiplayer.spec.ts` - 이미 올바르게 구현됨
- `street-transitions.spec.ts` - 이미 올바르게 구현됨
- `hand-ranking.spec.ts` - 이미 올바르게 구현됨
- `betting-buttons.spec.ts` - 이미 올바르게 구현됨
- `reconnect.spec.ts` - 이미 올바르게 구현됨
- `server-recovery.spec.ts` - 이미 올바르게 구현됨
- `card-exposure.spec.ts` - 이미 올바르게 구현됨
- `server-authority.spec.ts` - 이미 올바르게 구현됨
